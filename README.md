We integrate Hyperspectral (point spectra) and RGB images from herbarium specimens to build single modal and multimodal classification models and assess the classification accuracy of species identifications.

Each branch contains a different data modality (rgb, hyperspectral, scalograms from hyperspectral) and corresponding data utils, models, and evaluation for corresponding data modality.

